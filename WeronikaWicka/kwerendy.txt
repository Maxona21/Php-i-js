1.SELECT imie, kolor, cena FROM `klienci` WHERE topKlient = 1;
2.SELECT imie, cena FROM klienci WHERE termin like "2024-05-23%";
3.SELECT imie, kolor, ksztalty.ksztalt FROM klienci inner join ksztalty on klienci.ksztalty_id = ksztalty.id where ksztalty.ksztalt = "migdał";
4.alter table wzory add doplata int;